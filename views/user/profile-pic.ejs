<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Change Profile Picture</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <br>
    <div class="page-body pic-page">
        <img src="<%= user.image %>" alt="Profile Picture" width="200" height="200">
        <form action="/users/<%= user._id %>/profile-pic?_method=PUT" method="POST">
            <label for="profilePic">Image:</label> <small>(Must be URL with .png, .gif, .jpg or .jpeg extension)</small><br>
            <input type="url" name="profilePic" id="profilePic" pattern="\.(png|jpg|jpeg|gif)$" required>
            <button type="submit" id="profile-pic-edit-button">Update Profile Picture</button>
        </form>
        <br><br>
        <form action="/users/<%= user._id %>/profile-pic?_method=DELETE" method="POST">
            <button type="submit" class="delete-button">Delete Profile Picture</button>
        </form>
    </div>
</body>
</html>